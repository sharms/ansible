- hosts: localhost
  become: true
  tasks:
  - name: Install packages
    apt: 
      name: ['neovim', 'build-essential', 'git', 'ripgrep', 'fzf', 'nvidia-cuda-toolkit', 'zsh'] 

  - name: Install miniconda
    command: ./install_miniconda.sh
  
